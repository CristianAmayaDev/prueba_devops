const usuarios = require("../data/usuarios"); const obtenerUsuarios = () => { return usuarios; }; const obtenerUsuarioPorId = (id) => { return usuarios.find( usuario => usuario.id === Number(id) ); }; const crearUsuario = (nuevoUsuario) => { const nuevoId = usuarios.length > 0 ? Math.max(...usuarios.map(u => u.id)) + 1 : 1; const usuario = { id: nuevoId, ...nuevoUsuario }; usuarios.push(usuario); return usuario; }; const actualizarUsuario = (id, datosActualizados) => { const indice = usuarios.findIndex( usuario => usuario.id === Number(id) ); if (indice === -1) { return null; } usuarios[indice] = { ...usuarios[indice], ...datosActualizados, id: usuarios[indice].id }; return usuarios[indice]; }; 
const eliminarUsuario = (id) => {   const indice = usuarios.findIndex(     usuario => usuario.id === Number(id)   );    if (indice === -1) {     return null;   }    const usuarioEliminado = usuarios.splice(indice, 1);    return usuarioEliminado[0]; };  module.exports = {   obtenerUsuarios,   obtenerUsuarioPorId,   crearUsuario,   actualizarUsuario,   eliminarUsuario }; 
